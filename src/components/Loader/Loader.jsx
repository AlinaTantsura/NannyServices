import { LoaderContainer, LoaderStyled } from "./Loader.styled"

const Loader = () => {
    return (
      <LoaderContainer>
        <LoaderStyled />
    </LoaderContainer>
  )
}

export default Loader